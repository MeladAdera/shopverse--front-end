# Copy to `.env` for local development.
# When true, the app uses in-memory demo data (no backend required).
VITE_USE_DEMO_DATA=true

# Optional: real API base URL when demo mode is off
VITE_API_URL=http://localhost:5000/api
